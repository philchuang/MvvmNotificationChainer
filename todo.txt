* integration tests
	* 3 deep chain
	* 4 deep chain
	* INotifyCollectionChanged
* unit tests
	* notification chain manager
* v1.1
	* handle INotifyCollectionChanged
	* notification chain manager should be typed
	* NotificationChainPropertyAttribute - can call myNotificationChainManager.Observe (this) which also calls each property decorated with that attr to initialize the chain